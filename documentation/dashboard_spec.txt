# dashboard.spec
block_cipher = None

a = Analysis(['run_servers.py'],
             pathex=['path/to/your/dashboard'],
             binaries=[],
             datas=[('templates/*', 'templates'), ('static/*', 'static'), ('server.js', '.'), ('app.py', '.')],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped, cipher=block_cipher)
exe = EXE(pyz,
           a.scripts,
           [],
           exclude_binaries=True,
           name='DashboardApp',
           debug=False,
           bootloader_ignore_signals=False,
           strip=False,
           upx=True,
           console=True)
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               upx_exclude=[],
               name='DashboardApp')